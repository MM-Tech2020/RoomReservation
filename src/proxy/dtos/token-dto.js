export interface TokenDto {
  access_token: string;
  token_type: string;
  expires_in: string;
  refresh_token: string;
  id: string;
  name: string;
  email: string;
  issued: string;
  expires: string;
  subscriptionId: number;
  userId: number;
}
